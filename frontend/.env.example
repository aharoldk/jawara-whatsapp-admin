# Backend API key (must match BACKEND_API_KEY in your .env)
VITE_BACKEND_API_KEY=

# WAHA — only needed if you want the admin UI to call WAHA directly from the browser
# Leave blank when running via Docker (nginx proxies /waha → waha:3000)
VITE_WAHA_BASE_URL=
VITE_WAHA_API_KEY=
